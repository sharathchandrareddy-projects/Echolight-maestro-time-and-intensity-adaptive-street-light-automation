// Generates a blocking delay of 'dlyus' microseconds
void delay_us(unsigned int dlyus);

// Generates a blocking delay of 'dlyms' milliseconds
void delay_ms(unsigned int dlyms);

// Generates a blocking delay of 'dlys' seconds
void delay_s(unsigned int dlys);

